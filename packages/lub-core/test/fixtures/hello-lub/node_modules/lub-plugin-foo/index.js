'use strict';

const Command = require('lub-command');

class DevCommand  extends Command{
    constructor(rawArgv) {
        super(rawArgv)
    }
    
    run(context){
        console.log(context.rawArgv)
    }
}

module.exports = {
    dev: DevCommand
}