'use strict';

const Command = require('lub-command');

class DevCommand  extends Command{
    constructor(rawArgv) {
        super(rawArgv)
        this.usage = 'Usage: dev usage'
    }

    get description(){
        return 'dev command'
    }
    
    run(context){
        console.log(context.rawArgv)
    }
}

module.exports = {
    dev: DevCommand
}