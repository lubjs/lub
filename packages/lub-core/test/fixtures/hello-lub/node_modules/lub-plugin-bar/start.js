'use strict';

const Command = require('lub-command');

class StartCommand  extends Command{
    constructor(rawArgv,config) {
        super(rawArgv,config)
    }
    
    run(_,config){
        console.log('start config[env] is',config.env)
    }
}

module.exports = StartCommand