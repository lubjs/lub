'use strict';

const Command = require('lub-command');

class DanceCommand  extends Command{
    constructor(rawArgv) {
        super(rawArgv)
    }

    get description(){
        return 'concidance command'
    }
    
    run(){
        console.log('but kiki loves to dance');
    }
}

module.exports = {
    dance: DanceCommand
}